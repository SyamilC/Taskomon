# Taskomon

Taskomon is a productivity web app that turns tasks into free-flowing workspace bubbles. Instead of using a normal flat to-do list, users can arrange tasks visually, connect dependencies, track habits, run focused workflow sessions, and receive lightweight feedback from Taskomon, a companion that comments on progress.

The product is designed to work as a prototype without requiring backend setup. It can run fully in local.

## Running the Project Locally
### Clone the repository  
git clone https://github.com/SyamilC/Taskomon  
cd taskomon  

### Install dependencies  
npm install

### Run the development server  
npm run dev



Open the localhost link shown in the terminal, usually:  
http://localhost:5173

You can use the seeded local demo account:  
Email: demo@taskomon.local  
Password: demo123

You can also register a new local account inside the app.